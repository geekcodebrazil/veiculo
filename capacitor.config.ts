import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fabiano.controleveicular',
  appName: 'Controle Veicular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;